grpc:
  server:
    port: 0
server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
    prefer-ip-address: true
logging:
  level:
    root: INFO

spring:
  kafka:
    bootstrap-servers: localhost:9092
topic:
  collector:
    userevent:
      out: stats.user-actions.v1
